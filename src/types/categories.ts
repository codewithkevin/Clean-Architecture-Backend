const categoriesConstance = {
  funeral: "Funeral Events" as string,
  religious: "Religious Events" as string,
  wedding: "Wedding Events" as string,
  party: "Party Events" as string,
  educational: "Educational Events" as string,
  liveStreaming: "Live Streaming Events" as string,
  corperate: "Corperate Events" as string,
  classes: "Classes and Workshops" as string,
  conference: "Conference" as string,
  concerts: "Concerts" as string,
  festivals: "Festivals" as string,
  online: "Online Events" as string,
};

export default categoriesConstance;
